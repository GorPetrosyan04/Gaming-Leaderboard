# Gaming-Leaderboard
Build a command-line Gaming Leaderboard that ingests player scores over time, keeps a history per player, and can produce ranked leaderboards on demand using your heap sort (not the language’s sort). The system must not mutate the underlying histories when creating sorted outputs.
